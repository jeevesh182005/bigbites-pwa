Replace these placeholder PNG files with the final Big Bites food images using the exact same filenames.
The app imports every menu image from this folder, so keeping names unchanged prevents build errors.
